<div class="main-card mb-1 card">
    <div class="card-header">
        @if ($back <> '#')
            <a href="{{ url($back) }}" class="btn btn-outline-secondary btn-sm"><i class="lnr lnr-arrow-left"></i> Kembali</a>
        @endif
    </div>
</div>